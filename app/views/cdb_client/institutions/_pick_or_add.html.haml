- institution ||= nil
- exclude_institution_code ||= nil
- only_likely_countries ||= false
- default_country_code ||= nil
- required ||= false
- with_employer ||= false
- with_employer_address ||= false
- additional_field ||= false
- with_labels ||= false
- one_col ||= false
- whitelist_institute ||= false
- readonly ||= false
- country_code_field ||= :country_code
- institution_code_field ||= :institution_code
- institution_name_field ||= :institution_name
-# Optional preselection, used when another view already knows which institution should be chosen.
- selected_institution_code ||= nil
- selected_country_code ||= nil

- country_code = selected_country_code.presence || f.object.send(country_code_field)
- country_code ||= institution.country_code if institution.present?
- country_code ||= default_country_code

- institution_name_placeholder ||= t('placeholders.institution_name')
- label_class = required ? "required" : ""
- pending_field = institution_name_field.to_s.include?('second') ? :pending_second_institution_name : :pending_institution_name
- pending_name = f.object.respond_to?(pending_field) ? f.object.send(pending_field) : nil
- employer_name_field ||= :employer
- employer_name_placeholder ||= t('placeholders.employer')
- employer_address_placeholder ||= t('placeholders.employer_address')
- additional_field_placeholder ||= t("placeholders.#{additional_field}") if additional_field

- rowclass = additional_field ? "threecol" : "twocol unequal"

- institution_code_value = selected_institution_code.presence || f.object.send(institution_code_field)
-# Defensive: a value persisted as "pending:<name>" is an unresolved name, not a code.
- if institution_code_value.to_s.start_with?('pending:')
  - pending_name = institution_code_value.to_s.sub(/\Apending:/i, '') if pending_name.blank?
  - institution_code_value = nil
- institution_selection = Institution.for_selection(country_code, false, whitelist_institute, exclude_code: exclude_institution_code)
- if pending_name.present? && institution_code_value.blank?
  - institution_selection = [["#{pending_name} (unverified)", "pending:#{pending_name}"]] + institution_selection

- if only_likely_countries
  - country_options = Country.likely_for_selection
- else
  - country_options = Country.for_selection

-# When locked, resolve full names through the same preload cache the dropdown
-# uses, falling back to a label lookup, then to the raw code as a last resort.
- if readonly
  - country_obj   = country_code.present? ? Country.preloaded(country_code) : nil
  - country_label = country_obj&.name
  - country_label ||= (country_options.to_h.key(country_code) rescue nil)
  - country_label = country_code.to_s if country_label.blank?
  - inst_obj      = institution_code_value.present? ? Institution.preloaded(institution_code_value) : nil
  - inst_label    = inst_obj&.name_with_location
  - inst_label  ||= (institution_selection.to_h.key(institution_code_value) rescue nil)
  - inst_label    = institution_code_value.to_s if inst_label.blank?
  - employer_value = f.object.respond_to?(employer_name_field) ? f.object.send(employer_name_field) : nil

- picker_control_locals = {f: f, with_labels: with_labels, readonly: readonly, inst_label: inst_label, institution_code_field: institution_code_field, institution_code_value: institution_code_value, with_employer: with_employer, employer_value: employer_value, employer_name_field: employer_name_field, pending_name: pending_name, exclude_institution_code: exclude_institution_code, institution_selection: institution_selection, institution_name_field: institution_name_field, institution_name_placeholder: institution_name_placeholder, employer_name_placeholder: employer_name_placeholder}

- unless one_col
  %fieldset.institution_chooser{class: (readonly ? 'readonly locked' : nil), data: {role: ("institution" unless readonly), required: required}}
    - if whitelist_institute
      = f.hidden_field 'whitelisted', value: true, class: 'whitelist_institute'
    = f.hidden_field 'exclude_institution_code', value: exclude_institution_code, class: 'exclude_institution_code'
    %p{class: rowclass}
      %span.col.first>
        - if with_labels
          = f.label country_code_field
        - if readonly
          = text_field_tag nil, country_label, readonly: true, class: 'locked', tabindex: -1
          = f.hidden_field country_code_field, value: country_code
        - else
          = f.select country_code_field, country_options, {include_blank: t("placeholders.please_choose_country"), selected: country_code, disabled: "-"}, data: {key: country_code_field, role: 'country'}

      - select_cell_class = additional_field ? "col" : "col last"
      %span{class: select_cell_class}>
        = render "cdb_client/institutions/picker_control", picker_control_locals.merge(label_text: nil)

      - if additional_field
        %span.col.last>
          - if with_labels
            - if additional_field == :post
              = f.label additional_field, "Position"
            - else
              = f.label additional_field
          = f.text_field additional_field, placeholder: additional_field == :post ? "Position" : additional_field_placeholder

    - if with_employer && with_employer_address && !readonly
      %p.address{data: {role: "other"}}
        = f.text_area :employer_address, placeholder: employer_address_placeholder
    - elsif with_employer && with_employer_address && readonly && f.object.respond_to?(:employer_address) && f.object.employer_address.present?
      %p.address
        = f.text_area :employer_address, readonly: true, class: 'locked', tabindex: -1
- else
  %fieldset.institution_chooser{class: (readonly ? 'readonly locked' : nil), data: {role: ("institution" unless readonly), required: required}}
    - if whitelist_institute
      = f.hidden_field 'whitelisted', value: true, class: 'whitelist_institute'
    = f.hidden_field 'exclude_institution_code', value: exclude_institution_code, class: 'exclude_institution_code'
    %p.onecol.appl_form_onecol
      %span.col.first>
        - if with_labels
          = f.label country_code_field, 'Country'
        - if readonly
          = text_field_tag nil, country_label, readonly: true, class: 'locked', tabindex: -1
          = f.hidden_field country_code_field, value: country_code
        - else
          = f.select country_code_field, country_options, {include_blank: t("placeholders.please_choose_country"), selected: country_code, disabled: "-"}, data: {key: country_code_field, role: 'country'}
    %p.onecol.appl_form_onecol
      - select_cell_class = additional_field ? "col" : "col last"
      %span{class: select_cell_class}>
        = render "cdb_client/institutions/picker_control", picker_control_locals.merge(label_text: 'University')

      - if additional_field
        %p.onecol.appl_form_onecol
          %span.col.last>
            - if with_labels
              - if additional_field == :post
                = f.label additional_field, "Position"
              - else
                = f.label additional_field
            = f.text_field additional_field, placeholder: additional_field == :post ? "Position" : additional_field_placeholder

    - if with_employer && with_employer_address && !readonly
      %p.address{data: {role: "other"}}
        = f.text_area :employer_address, placeholder: employer_address_placeholder
    - elsif with_employer && with_employer_address && readonly && f.object.respond_to?(:employer_address) && f.object.employer_address.present?
      %p.address
        = f.text_area :employer_address, readonly: true, class: 'locked', tabindex: -1