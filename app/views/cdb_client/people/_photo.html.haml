- photo ||= nil
- cssclass = "mugshot"
- person ||= photo || @person
- if person
  - if person.icon.present?
    - styles = "background-image: url(\"#{person.icon}\")"
  - else
    - styles = ""

  %div{class: cssclass, style: styles}
